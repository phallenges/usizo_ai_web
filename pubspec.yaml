name: usizo_ai
description: Offline-first symptom guidance and wellness marketplace demo.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.2
  url_launcher: ^6.3.1
  onnxruntime: ^1.4.1
  vector_math: ^2.1.4
  flutterwave_standard: ^1.1.0
  http: ^1.1.0
  uuid: ^4.0.0
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/remedies.json
    - assets/models/all_minilm_l12_v2.onnx

